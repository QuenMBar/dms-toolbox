import React from 'react'

export default function DmPage() {
    return (
      <div>
        <h1>Your Campaigns</h1>
            {/* {props.camps.map((camp) => {
                return (
                  <div key={camp.id}>
                    <Link to={`/campaign/${props.camp.name}`}>
                      {camp.name}
                    </Link>
                   </div>
                );
            })} */}
        </div>
    )
}
